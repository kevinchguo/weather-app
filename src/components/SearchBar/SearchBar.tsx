const SearchBar: React.FC = () => (
    <div>
        <label htmlFor="searchBar">
            <p>Search city</p>
            <input id="searchBar" type="text" />
        </label>
    </div>
);

export default SearchBar;
