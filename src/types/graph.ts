export interface GraphSize {
    width: number;
    height: number;
    padding: number;
    legendx: number;
    legendy: number;
}

export interface GraphZoom {
    x: [number, number];
}

export interface GraphFont {
    fontFamily: string;
}
